# Operis API URL (remote server for auth, billing, chat routing)
VITE_OPERIS_API_URL=http://localhost:3025/api

# Gateway URL (optional, for direct gateway mode - leave empty for same origin)
# VITE_GATEWAY_URL=http://localhost:18792

# Gateway Token (optional, for direct gateway mode without Operis auth)
# VITE_GATEWAY_TOKEN=your-gateway-token
